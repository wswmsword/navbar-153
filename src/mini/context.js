import { createContext } from "react";

export const ContextForMiniContent = createContext({});
export const ContextForMiniItem = createContext({});
export const ContextForMiniTrigger = createContext({});
export const ContextForMiniMenu = createContext({});
export const ContextForMiniToggle = createContext({});